# Automations

Version 0.3.1 schedules no recurring jobs by default. Search, inbox triage and weekly review automations may be offered later as opt-in blueprints so installation never silently creates external activity.
